# Supabase project: campfire-songbook
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_xxx

# Any random string. Salts the IP hash used for submission rate limiting,
# so the stored hashes cannot be reversed with a rainbow table.
SUBMISSION_SALT=change-me
